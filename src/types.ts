export type PlayerType = {
    id: string;
    name: string;
    status: string;
    matches: number;
    wins: number;
    waitingList: string[];
    requestList: string[];
    choice: string;
    opponent: string;
};
